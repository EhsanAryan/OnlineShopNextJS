const adminHandler = (req, res) => {
    // if(req.status === "POST") {
    //     console.log("post method!");
    // }
    res.json({
        message: "Done!"
    });
}

export default adminHandler;